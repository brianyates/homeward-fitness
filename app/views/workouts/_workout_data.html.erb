<p><span class="workout-icon dib text-main"><i class="fas fa-address-card"></i></span><span class="text-bold">Workout Instructor: </span><a target="_blank" href="/instructors/<%= w.instructor_id %>"><%= w.instructor_name %></a></p>
<p><span class="workout-icon dib text-green"><i class="fas fa-clipboard-list"></i></span><span class="text-bold">Description: </span><%= w.workout_description %></p>
<p><span class="workout-icon dib text-purple"><i class="fas fa-hourglass-start"></i></span><span class="text-bold">Duration: </span><%= w.duration.present? ? pluralize(w.duration, "minute") : "As fast as you can do it!" %></p>
<p><span class="workout-icon dib text-pink"><i class="fas fa-chart-area"></i></span><span class="text-bold">Difficulty: </span><%= w.difficulty %></p>
<p><span class="workout-icon dib text-primary"><i class="fas fa-paperclip"></i></span><span class="text-bold">Equipment required: </span><%= w.required_equipment %></p>
<div class="<%= 'fitness-points' if t==0 %>"><span class="workout-icon dib text-orange"><i class="fas fa-star"></i></span><span class="text-bold">Fitness Points:</span>
	<% if w.workout_type == 1 %>
		<div><ul><li><span class="text-bold"><%= w.points_per_workout %> points</span> per workout completed</span></li></ul></div>
	<% elsif w.workout_type == 2 %>
		<ul>
		<% w.exercises.each do |ex| %>
			<% if ex.exercise_description == "Round" %>
				<li class="text-bold"><%= ex.points_per_rep %> Fitness Points per round completed</li>
			<% else %>
				<li><span class="text-bold"><%= ex.exercise_description %>s</span> - <%= pluralize(ex.points_per_rep, "Fitness Point") %> per rep
			<% end %>
		<% end %>
	<% elsif w.workout_type == 3 %>
		<div><%= cutoffs(w.point_cutoffs) %></div>
	<% end %>
</div>